// 公共方法
export class CommonHandle { //类文件模板
    constructor(methodsName: Function) { //初始化super(方法名称)
        methodsName();
    };
    props = { //数据存放对象
    };
    methods = { //方法存放对象
    };
}